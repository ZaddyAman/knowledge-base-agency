# Agent instructions

## Agent skills

### Issue tracker

Issues, specs, and delivery tickets are tracked in GitHub Issues. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the canonical `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix` roles. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository with `CONTEXT.md` and root-level `docs/adr/`. See `docs/agents/domain.md`.

### Delivery workflow

Follow the phase gates and ceremony budget in `docs/process/skill-playbook.md`.
